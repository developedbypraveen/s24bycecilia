{% comment %}
  S24 footer — one section with column blocks + bottom bar.
  Footer group: plum strip (separate) → this section only.
{% endcomment %}

{{ 'component-list-social.css' | asset_url | stylesheet_tag }}
{{ 'component-list-payment.css' | asset_url | stylesheet_tag }}
{{ 'component-newsletter.css' | asset_url | stylesheet_tag }}
{{ 'component-localization-form.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .s24-ft-{{ section.id }} {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round }}px;
  }
  @media screen and (min-width: 750px) {
    .s24-ft-{{ section.id }} {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }
{%- endstyle -%}

{%- liquid
  assign has_social = false
  if settings.social_facebook_link != blank or settings.social_instagram_link != blank or settings.social_youtube_link != blank or settings.social_twitter_link != blank or settings.social_tiktok_link != blank or settings.social_pinterest_link != blank
    assign has_social = true
  endif
-%}

<footer class="s24-ft s24-ft-{{ section.id }}">
  <div class="page-width">
    {%- if section.blocks.size > 0 -%}
      <div class="s24-ft__grid">
        {%- for block in section.blocks -%}
          <div class="s24-ft__col s24-ft__col--{{ block.type | replace: '@', '' }}" {{ block.shopify_attributes }}>
            {%- case block.type -%}
              {%- when 'brand' -%}
                <a href="{{ routes.root_url }}" class="s24-ft__logo">
                  {%- if block.settings.logo != blank -%}
                    {{
                      block.settings.logo
                      | image_url: width: 360
                      | image_tag: loading: 'lazy', widths: '120, 180, 240, 360', alt: shop.name
                    }}
                  {%- elsif settings.logo != blank -%}
                    {{
                      settings.logo
                      | image_url: width: 360
                      | image_tag: loading: 'lazy', widths: '120, 180, 240, 360', alt: shop.name
                    }}
                  {%- else -%}
                    <span class="s24-ft__logo-text">{{ shop.name }}</span>
                  {%- endif -%}
                </a>
                {%- if block.settings.text != blank -%}
                  <div class="s24-ft__brand-text rte">{{ block.settings.text }}</div>
                {%- endif -%}
                {%- if block.settings.show_social and has_social -%}
                  {%- render 'social-icons', class: 's24-ft__social' -%}
                {%- endif -%}

              {%- when 'menu' -%}
                {%- if block.settings.heading != blank -%}
                  <h2 class="s24-ft__heading">{{ block.settings.heading | escape }}</h2>
                {%- endif -%}
                {%- if block.settings.menu != blank -%}
                  <ul class="s24-ft__menu list-unstyled">
                    {%- for link in block.settings.menu.links -%}
                      <li>
                        <a href="{{ link.url }}">{{ link.title | escape }}</a>
                      </li>
                    {%- endfor -%}
                  </ul>
                {%- endif -%}

              {%- when 'newsletter' -%}
                {%- if block.settings.heading != blank -%}
                  <h2 class="s24-ft__heading">{{ block.settings.heading | escape }}</h2>
                {%- endif -%}
                {%- if block.settings.text != blank -%}
                  <p class="s24-ft__newsletter-text">{{ block.settings.text | escape }}</p>
                {%- endif -%}
                {%- form 'customer', id: 'ContactFooterS24', class: 's24-ft__form newsletter-form' -%}
                  <input type="hidden" name="contact[tags]" value="newsletter">
                  <div class="s24-ft__field field">
                    <input
                      id="NewsletterForm--{{ section.id }}"
                      type="email"
                      name="contact[email]"
                      class="field__input"
                      value="{{ form.email }}"
                      aria-required="true"
                      autocorrect="off"
                      autocapitalize="off"
                      autocomplete="email"
                      placeholder="Enter your email"
                      required
                    >
                    <label class="field__label" for="NewsletterForm--{{ section.id }}">Enter your email</label>
                  </div>
                  <button type="submit" class="s24-ft__subscribe" name="commit">
                    {{ block.settings.button_label | default: 'Subscribe' | escape }}
                  </button>
                  {%- if form.errors -%}
                    <small class="form__message">
                      {{ form.errors.translated_fields.email | capitalize }}
                      {{ form.errors.messages.email }}
                    </small>
                  {%- endif -%}
                  {%- if form.posted_successfully? -%}
                    <h3 class="form__message" tabindex="-1" autofocus>{{ 'newsletter.success' | t }}</h3>
                  {%- endif -%}
                {%- endform -%}
            {%- endcase -%}
          </div>
        {%- endfor -%}
      </div>
    {%- endif -%}

    <div class="s24-ft__bottom">
      <div class="s24-ft__copy">
        <small>
          &copy; {{ 'now' | date: '%Y' }} s24bycecilia. All rights reserved.
        </small>
        {%- if section.settings.show_policy -%}
          <span class="s24-ft__policies">
            {%- for policy in shop.policies -%}
              {%- if policy != blank -%}
                <a href="{{ policy.url }}">{{ policy.title | escape }}</a>
              {%- endif -%}
            {%- endfor -%}
          </span>
        {%- endif -%}
      </div>

      {%- if section.settings.payment_enable -%}
        <div class="s24-ft__payments">
          <span class="visually-hidden">{{ 'sections.footer.payment' | t }}</span>
          <ul class="list list-payment" role="list">
            {%- for type in shop.enabled_payment_types -%}
              <li class="list-payment__item">
                {{ type | payment_type_svg_tag: class: 'icon icon--full-color' }}
              </li>
            {%- endfor -%}
          </ul>
        </div>
      {%- endif -%}

      <div class="s24-ft__locale">
        {%- if section.settings.enable_country_selector and localization.available_countries.size > 1 -%}
          <localization-form>
            {%- form 'localization', id: 'FooterCountryFormS24', class: 'localization-form' -%}
              <div>
                <h2 class="visually-hidden" id="FooterCountryLabelS24">{{ 'localization.country_label' | t }}</h2>
                {%- render 'country-localization', localPosition: 'FooterCountryS24' -%}
              </div>
            {%- endform -%}
          </localization-form>
        {%- endif -%}
        {%- if section.settings.enable_language_selector and localization.available_languages.size > 1 -%}
          <localization-form>
            {%- form 'localization', id: 'FooterLanguageFormS24', class: 'localization-form' -%}
              <div>
                <h2 class="visually-hidden" id="FooterLanguageLabelS24">{{ 'localization.language_label' | t }}</h2>
                {%- render 'language-localization', localPosition: 'FooterLanguageS24' -%}
              </div>
            {%- endform -%}
          </localization-form>
        {%- endif -%}
      </div>
    </div>
  </div>
</footer>

<a href="#MainContent" class="s24-back-top" data-s24-back-top aria-label="Back to top">
  <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true">
    <path d="M7 11V3M3.5 6.5L7 3l3.5 3.5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
  </svg>
</a>

{% schema %}
{
  "name": "S24 footer",
  "tag": "section",
  "class": "section",
  "enabled_on": { "groups": ["footer"] },
  "settings": [
    {
      "type": "header",
      "content": "Bottom bar"
    },
    {
      "type": "checkbox",
      "id": "payment_enable",
      "label": "Show payment icons (center)",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_language_selector",
      "label": "Show language selector",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "enable_country_selector",
      "label": "Show currency / country selector",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_policy",
      "label": "Show policy links",
      "default": false
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 64
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 28
    }
  ],
  "blocks": [
    {
      "type": "brand",
      "name": "Brand (logo + social)",
      "limit": 1,
      "settings": [
        {
          "type": "image_picker",
          "id": "logo",
          "label": "Logo",
          "info": "Uses theme logo if empty"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Brand text",
          "default": "<p>African luxury fashion for women who love elegance. Heritage print, modern cut — designed in Canada.</p>"
        },
        {
          "type": "checkbox",
          "id": "show_social",
          "label": "Show social icons",
          "default": true
        }
      ]
    },
    {
      "type": "menu",
      "name": "Menu",
      "limit": 3,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Quick Links"
        },
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu",
          "default": "footer"
        }
      ]
    },
    {
      "type": "newsletter",
      "name": "Stay in touch",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading",
          "label": "Heading",
          "default": "Stay in touch"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Subscribe to get special offers and updates."
        },
        {
          "type": "text",
          "id": "button_label",
          "label": "Button label",
          "default": "Subscribe"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "S24 footer",
      "blocks": [
        { "type": "brand" },
        {
          "type": "menu",
          "settings": { "heading": "Quick Links" }
        },
        {
          "type": "menu",
          "settings": { "heading": "Information" }
        },
        { "type": "newsletter" }
      ]
    }
  ]
}
{% endschema %}
